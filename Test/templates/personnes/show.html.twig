{% extends 'base.html.twig' %}

{% block title %}Personnes{% endblock %}

{% block body %}
    <h1>Personnes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personne.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ personne.Prenom }}</td>
            </tr>
            <tr>
                <th>DDN</th>
                <td>{{ personne.DDN ? personne.DDN|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('personnes_index') }}">back to list</a>

    <a href="{{ path('personnes_edit', {'id': personne.id}) }}">edit</a>

    {{ include('personnes/_delete_form.html.twig') }}
{% endblock %}
